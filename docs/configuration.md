# Configuration

All configuration comes from environment variables, read once by
[`RabbitMQConfig.from_env`][langchain_rabbitmq.RabbitMQConfig.from_env].
You can also pass keyword arguments to construct a config explicitly.

::: langchain_rabbitmq.config
    options:
      show_root_heading: false
      members_order: source
